# S5 core-loop gate

Date: 2026-08-31

## Scope

This is the S5 shell checkpoint only. It does not claim the S16 release gate.

## Automated evidence

- Unity editor: `6000.3.23f1` at `/home/duongduy/Unity/Hub/Editor/6000.3.23f1/Editor/Unity`.
- Compile-only command: `Unity -batchmode -nographics -projectPath . -quit` — passed after the S5 scene/controller changes; no `error CS` or script compilation failure remained.
- `git diff --check` — passed.
- Android build command: `KMA.EditorTools.BuildScript.BuildAndroid` — Unity reported `Build Finished, Result: Success`; APK `Builds/Android/kma-s5.apk` was produced at 31.5 MB. The editor log also contains 3 non-fatal counted errors and 21 warnings, so this is packaging evidence, not a pristine-log claim.
- PlayMode command was attempted with the `CoreLoopTests` filter. Unity exited without producing XML because its IL post-processing build server requires a `dotnet` executable, which is unavailable in this environment (`The application 'build-server' does not exist`, `No .NET SDKs were found`).

## Device gate

Not executed in this environment. Device model, Android API, APK install, and the Menu → Map → subject → punishment → retry → Game Over, seven-subject unlock, persistence, pause, and calibration flows remain to be verified on hardware.

## Known S5 limitations

- The five new subjects use `PlaceholderMinigameController` debug Pass/Fail controls by design; replace them before S16.
- The shell scenes are generated by `KMA.EditorTools.S5SceneConfigurator.Generate` and use runtime route bindings through `S5ShellSceneController`.
- No claim of final art/audio quality or production minigame behavior is made here.
